# Skill Registry

## Project Standards (auto-resolved)

### TypeScript Best Practices
- Load: `typescript-best-practices`
- Trigger: `.ts` files, `.astro` script tags
- Rules:
  - Use functional patterns (read-only, pure functions)
  - Strict type safety, no `any`
  - Async/Await for all asynchronous operations

### Web Design Guidelines
- Load: `web-design-guidelines`
- Trigger: `.astro` components
- Rules:
  - Ensure accessibility (ARIA labels, semantic HTML)
  - Responsive design (mobile-first)
  - Consistent spacing and typography

### Vercel React Best Practices
- Load: `vercel-react-best-practices`
- Trigger: React components (if any)
- Rules:
  - Optimize for performance
  - Proper data fetching patterns
